providers:
  # Placeholder pricing — NOT claimed accurate. Tune per org contract.
  anthropic:
    models:
      claude-sonnet:
        inputPer1M: 3.0
        outputPer1M: 15.0
      claude-opus:
        inputPer1M: 15.0
        outputPer1M: 75.0
      claude-haiku:
        inputPer1M: 0.8
        outputPer1M: 4.0

  openai:
    models:
      gpt-4o:
        inputPer1M: 2.5
        outputPer1M: 10.0
      gpt-4o-mini:
        inputPer1M: 0.15
        outputPer1M: 0.6

  google:
    models:
      gemini-pro:
        inputPer1M: 1.25
        outputPer1M: 5.0

  other:
    models:
      default:
        inputPer1M: 1.0
        outputPer1M: 1.0
