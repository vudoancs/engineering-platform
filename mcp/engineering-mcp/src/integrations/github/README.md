# github integration

Purpose: READ-ONLY GitHub REST API client and service used by MCP tools.

Credentials come from environment variables (`GITHUB_TOKEN`, `GITHUB_API_URL`).
Repository access is project-scoped via `ProjectConfigService`.
